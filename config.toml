# The URL the site will be built for
base_url = "https://wiserfz.github.io"

# The site title and description; used in feeds by default
title = "wiser's blog"
description = "👋 Welcome to my plog! I'm wiser, a software developer who loves to learn and share."

# The default language; used in feeds
default_language = "en"

# The site theme to use
theme = "apollo"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", the generated HTML files are minified
minify_html = true

# When set to "true", a feed is automatically generated
generate_feeds = true

# The default author for pages
author = "wiser"

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [
    {name = "tags"}, # you can have taxonomies with the same name in multiple languages
    {name = "categories", paginate_by = 5},  # 5 items per page for a term
]

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[search]
include_title = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true

[extra]
# Whether to enable the table of contents
toc = true
# Put all your custom variables here
theme = "toggle"

favicon = "/img/favicons/favicon.ico"

# Whether to enable fancy code blocks
fancy_code = true

# Whether to enable MathJax support
mathjax = true
mathjax_dollar_inline_enable = true

menu = [
    { name = "/home", url = "/", weight = 1 },
    { name = "/posts", url = "/posts", weight = 2 },
    { name = "/tags", url = "/tags", weight = 99 },
    { name = "/about", url = "/about", weight = 100 },
]

socials = [
    # { name = "rss", url = "https://wiserfz.github.io/atom.xml", icon = "rss" },
    { name = "email", url = "mailto:wiserfz810@gmail.com", icon = "email" },
    { name = "github", url = "https://github.com/wiserfz/", icon = "github" },
]
